import test from 'node:test'; import assert from 'node:assert/strict'; import fs from 'node:fs'; import { GameEngine } from '../src/engine/GameEngine.js';
const raw=JSON.parse(fs.readFileSync(new URL('../src/data/generated/cards.json',import.meta.url),'utf8'));
const names=['Toxic Deluge','Underworld Dreams','Untimely Malfunction','Urabrask // The Great Work','Valgavoth, Harrower of Souls','Virtue of Courage // Embereth Blaze','Vorrac Battlehorns',"Witch's Clinic",'Zo-Zu the Punisher'];
test('Valgavoth integration batch 8 is reachable in the real runtime database',()=>{const deck={id:'d',name:'d',commander:'hakbal',cards:[{id:'hakbal',quantity:1}]};const e=new GameEngine(deck,[{...deck,id:'o',name:'o'}],raw,{validateDecks:false,rng:()=>0.5});for(const name of names){const c=Object.values(e.db).find(x=>x.name===name);assert.ok(c,`${name} missing`);assert.equal(c.certificationEligible,true,`${name} not targeted`);assert.match(c.targetedImplementation,/^rakdos-deck-/);}});
