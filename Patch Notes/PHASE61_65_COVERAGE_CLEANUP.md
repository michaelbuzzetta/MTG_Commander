# Phase 61–65 — coverage cleanup

This checkpoint adds explicit CR-layer/dependency helpers, copy/control edge-case state, census-driven remaining-card classification, Oracle-family clustering, and a narrow individual-card implementation registry. The compiler gains reusable families for layer-6 ability removal, base P/T setting, token copies, temporary control, and basic landcycling. Phase 65 deliberately does not claim universal coverage: the definitive full-catalog proof is Step 66, where every catalog entry must be compiled/audited and every remaining exception classified.
